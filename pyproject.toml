[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "bkgco"
version = "0.1.0"
description = "Python wrapper for the BK maxflow and GCO C++ libraries"
requires-python = ">=3.13"
dependencies = ["numpy>=1.20"]

[tool.setuptools]
packages = ["bkgco"]
